# Copy this file to .env (same directory as docker-compose.yml) and set
# a real value before running docker-compose for anything beyond local
# throwaway testing. Generate one with: openssl rand -base64 48

JWT_SECRET=change-this-to-a-long-random-secret-in-production-min-256-bits
